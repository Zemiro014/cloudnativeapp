name: pipeline

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Get Source Code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'npm'

      - name: Install Backend Dependencies
        run: npm --prefix backend install

      - name: Install dependencies of frontend
        run: |
          cd frontend
          npm install
      - name: Build Frontend (Next.js App)
        run: |
          cd frontend
          npm run build
      - name: Create artifact directory and copy files
        run: |
          mkdir -p frontend-build
          cp -r frontend/.next frontend/package.json frontend/next.config.mjs frontend/node_modules frontend-build/
          # Copy necessary files for a standalone deployment. For static export use "out" folder.

      - name: Upload Frontend Build
        uses: actions/upload-artifact@v4
        with:
         name: frontend-build-artifact
         path: frontend-build/

  test:
   needs: build
   runs-on: ubuntu-latest
   steps:
    - name: Run App tests
      run: echo "Tests go here"

  deploy:
    needs: test
    runs-on: ubuntu-latest
    steps:
     - name: Deploy App Frontend
       run: echo "Deploy Frontend here"